<h1>HTML5: Form Validation</h1>
 
<table>
  <tr>    
    <th>Username</th>
    <th>Password Digest</th>
    <th>Created_at</th>      
  </tr>
 
  <% @ch0714validations.each do |ch0714validation| %>
    <tr>      
      <td><%= ch0714validation.username %></td>
      <td><%= ch0714validation.password_digest %></td>
      <td><%= ch0714validation.created_at %></td>
    </tr>
  <% end %>
</table>

<br />
<%= link_to 'Add new', new_ch0714validation_path %><br />
<%= link_to 'Go home', root_path %>
